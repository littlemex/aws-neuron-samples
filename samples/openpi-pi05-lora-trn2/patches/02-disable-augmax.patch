diff --git a/src/openpi/models/model.py b/src/openpi/models/model.py
index 29618b4..f91e680 100644
--- a/src/openpi/models/model.py
+++ b/src/openpi/models/model.py
@@ -165,8 +165,7 @@ def preprocess_observation(
             logger.info(f"Resizing image {key} from {image.shape[1:3]} to {image_resolution}")
             image = image_tools.resize_with_pad(image, *image_resolution)

-        if train:
-            # Convert from [-1, 1] to [0, 1] for augmax.
+        if train and False:  # PATCH_NEURON_NO_AUGMAX
             image = image / 2.0 + 0.5

             transforms = []
