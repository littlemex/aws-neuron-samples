# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Node / Next.js
node_modules/
.next/
.turbo/
out/
dist/
*.tsbuildinfo

# CDK
infra/cdk.out/
infra/.cdk.staging/
# cdk.context.json は VPC lookup の結果を環境固有 ID (account / vpc / subnet)
# で焼き込むため VCS 管理外。ルート .gitignore に既に入っているが、サブ
# プロジェクトの .gitignore でも明示して取りこぼしを防ぐ。
infra/cdk.context.json

# 一時運用スクリプトが書き出すバックアップ (環境固有 ID を含むので track しない)
infra/scripts/.cf-backup/

# Editor / OS
.DS_Store
.vscode/
.idea/

# Local env
.env
.env.local
.env.*.local
